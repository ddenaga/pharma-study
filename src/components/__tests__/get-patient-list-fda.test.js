import { render, screen, cleanup } from '@testing-library/react';

//import AssignDrugs from '../../pages/fda/assign-drugs.jsx';

test('should not see PII and instead see nulls', () => {
    expect(true).toBe(true);
})
